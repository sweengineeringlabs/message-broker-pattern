# message-broker-pattern-contract

Trait/type surface only: `MessageBroker`, `Validator`, `Message`, `BackendKind`, and the
`*Request`/`*Response` DTOs. Zero implementation.

**Status:** placeholder scaffold — real content is being ported from
[`edge-message-broker`](https://github.com/sweengineeringlabs/edge-message-broker)'s `api/`
module tree. See this repo's migration issue (linked from
[edge-message-broker#6](https://github.com/sweengineeringlabs/edge-message-broker/issues/6)).
