# message-broker-pattern-contract

Trait/type surface only: `MessageBroker`, `Validator`, `Message`, `BackendKind`, and the
`*Request`/`*Response` DTOs. Zero implementation of any named backend.

Ported from [`edge-message-broker`](https://github.com/sweengineeringlabs/edge-message-broker)'s
`api/` module tree per
[edge-message-broker#6](https://github.com/sweengineeringlabs/edge-message-broker/issues/6).
